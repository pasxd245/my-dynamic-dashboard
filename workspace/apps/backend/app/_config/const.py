"""Application-wide constants that are NOT user-overridable.

Different surface from `Fields` (which IS overridable via config).
R28 seeds the minimal set; future rounds extend as concrete needs
surface.
"""

from __future__ import annotations


class Const:
    APP_NAME = "my-dynamic-dashboard-backend"
