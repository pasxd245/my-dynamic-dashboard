node_modules
.code-review-graph
dist
out
build
coverage
package-lock.json
yarn.lock
pnpm-lock.yaml
.husky/_
LICENSE
# Vendored Python deps + autogenerated caches
**/.venv
**/.pytest_cache
# Build/dev temp area (PIDs, logs)
tmp
# js-tmpl Handlebars templates — prettier doesn't understand `{{...}}` in
# context-specific files (.env.hbs, .ts.hbs); leave formatting to the
# template author (R27).
**/*.hbs
