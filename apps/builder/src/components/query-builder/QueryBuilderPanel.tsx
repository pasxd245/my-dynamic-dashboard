import type { ReactElement } from "react";

export default function QueryBuilderPanel(): ReactElement {
  return (
    <section>
      <h2>Query Builder</h2>
      <p>Query builder scaffold is ready for US1 implementation.</p>
    </section>
  );
}
