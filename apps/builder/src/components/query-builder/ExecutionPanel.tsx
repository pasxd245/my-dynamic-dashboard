import type { ReactElement } from "react";

export default function ExecutionPanel(): ReactElement {
  return (
    <section>
      <h3>Execution</h3>
      <p>Full query execution output and errors will render here.</p>
    </section>
  );
}
