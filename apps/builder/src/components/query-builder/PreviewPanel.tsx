import type { ReactElement } from "react";

export default function PreviewPanel(): ReactElement {
  return (
    <section>
      <h3>Preview</h3>
      <p>Preview results and timeout states will render here.</p>
    </section>
  );
}
